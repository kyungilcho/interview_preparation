# 매스프레소 면접 대비 — 핵심 기본 지식 정리 (시니어 레벨)

> **목적:** 기존 `면접준비_매스프레소.md`를 보완하는 "기초부터 시니어까지" 빠른 복습 시트
> **범위:** JavaScript → TypeScript → React → React Native
> **기존 문서 오류:** 총 5건 발견 → 기존 문서 직접 수정 완료 + 아래 팩트체크 섹션에 정리

---

## 📋 팩트체크: 기존 면접준비 문서에서 수정한 사항

> [!WARNING]
> 아래 5건은 이미 `면접준비_매스프레소.md`에 직접 반영 완료

| # | 위치 | 수정 내용 |
|---|---|---|
| 1 | Q1 (New Arch) | "두 가지" → **네 가지** (Codegen, JSI 분리 설명 + Bridgeless Mode + View Flattening 추가) |
| 2 | Q3 꼬리 3-1 | "React 19에서 강화되고 있습니다" → **React Compiler 1.0 안정 릴리스 (2025.10.7)**, React 17+ 호환, Next.js/Vite/Expo 통합 |
| 3 | Q3 꼬리 3-2-1 | "RecyclerView 기반" → **RecyclerListView 기반** (JS 레벨 cell recycling, 네이티브 RecyclerView와 다름) |
| 4 | Q5 (CodePush) | Microsoft App Center **2025년 3월 31일 서비스 종료** 사실 추가. EAS Update, Shorebird 대안 언급 |
| 5 | Q6 꼬리 6-3-1 | `gcTime` (이전 cacheTime) — 이 부분은 정확 ✅ (React Query v5에서 변경) |

---

## Part 1. JavaScript 핵심 (시니어 레벨)

### 1.1 이벤트 루프 (Event Loop) — 완전 분해

> [!IMPORTANT]
> 면접에서 가장 자주 나오는 JS 기초 질문. "Promise와 setTimeout 실행 순서"를 설명하라는 변형이 많음.

#### 실행 순서 규칙
```
┌──────────────────────────────────────────────────────┐
│                     Call Stack                        │ ← 동기 코드 실행
│      (비면 다음 단계로)                                  │
├──────────────────────────────────────────────────────┤
│                  Microtask Queue                      │ ← 전부 소진할 때까지
│  Promise.then/catch/finally                           │
│  queueMicrotask()                                    │
│  MutationObserver 콜백                                │
│  (마이크로태스크가 마이크로태스크를 생성하면 그것도 여기서)     │
├──────────────────────────────────────────────────────┤
│             (렌더링이 필요하면 여기서 실행)                 │
│  requestAnimationFrame 콜백                           │
│  Layout / Paint                                      │
├──────────────────────────────────────────────────────┤
│                  Macrotask Queue                      │ ← 하나만 실행
│  setTimeout / setInterval                             │
│  I/O 완료 콜백                                        │
│  MessageChannel                                      │
└──────────────────────────────────────────────────────┘
      ↑ 다시 Call Stack으로 (루프 반복)
```

1. **콜 스택** → 동기 코드를 전부 실행
2. **마이크로태스크 큐** → 콜 스택이 비면, 마이크로태스크를 **전부** 소진
3. **렌더링** → 필요한 경우 `requestAnimationFrame` → Layout → Paint
4. **매크로태스크 큐** → **하나만** 실행
5. **반복**

```javascript
console.log('1');                        // 동기
setTimeout(() => console.log('2'), 0);   // 매크로태스크
Promise.resolve().then(() => {
  console.log('3');                      // 마이크로태스크
  Promise.resolve().then(() => console.log('4')); // 마이크로태스크 내 마이크로태스크
});
console.log('5');                        // 동기

// 출력: 1 → 5 → 3 → 4 → 2
```

#### 심화 예제 (면접 단골)
```javascript
console.log('start');

setTimeout(() => {
  console.log('timeout1');
  Promise.resolve().then(() => console.log('promise inside timeout'));
}, 0);

setTimeout(() => {
  console.log('timeout2');
}, 0);

Promise.resolve().then(() => console.log('promise1'));
Promise.resolve().then(() => console.log('promise2'));

console.log('end');

// 출력:
// start → end → promise1 → promise2 → timeout1 → promise inside timeout → timeout2
//
// 핵심: timeout1 실행 후, timeout2가 아니라 "promise inside timeout"이 먼저!
// 이유: 매크로태스크(timeout1) 하나 실행 후, 마이크로태스크 큐를 다시 전부 소진하기 때문
```

**시니어 포인트:**
- 마이크로태스크가 마이크로태스크를 계속 생성하면 **starvation** 발생 (UI 렌더링이 영원히 안됨)
- `requestAnimationFrame`은 매크로태스크가 아니라 **렌더링 단계 직전**에 실행
- Node.js의 `process.nextTick`은 마이크로태스크보다도 먼저 실행 (Node 전용)
- `queueMicrotask()`로 직접 마이크로태스크를 등록할 수 있음

---

### 1.2 클로저 (Closure) — 원리부터 실전까지

#### 기본 원리
```javascript
function createCounter() {
  let count = 0;                    // 자유 변수
  return {
    increment: () => ++count,       // count를 클로저로 캡처
    getCount: () => count,
  };
}
const counter = createCounter();
counter.increment(); // 1
counter.increment(); // 2
// createCounter의 실행 컨텍스트는 끝났지만,
// 반환된 함수들이 count를 참조하므로 GC되지 않음
```

#### 렉시컬 환경 (Lexical Environment)
```
createCounter 호출 시:
┌─────────────────────────┐
│  Lexical Environment    │
│  count: 0               │ ← 이 환경이 GC되지 않고 유지됨
│  outer: Global Env      │
└─────────────────────────┘
         ↑
    increment(), getCount() 함수가 참조
```

**핵심:** 함수가 정의된 시점의 렉시컬 환경(변수 스코프)에 대한 참조를 유지하는 것. 실행 시점이 아니라 **정의 시점**의 스코프를 기억한다.

#### React에서의 Stale Closure 문제
```javascript
function Counter() {
  const [count, setCount] = useState(0);

  useEffect(() => {
    const id = setInterval(() => {
      console.log(count);    // 항상 0! (초기 렌더링 시점의 count를 캡처)
      setCount(count + 1);   // 항상 0 + 1 = 1
    }, 1000);
    return () => clearInterval(id);
  }, []);  // 빈 의존성 배열 → 최초 1회만 실행, count=0 시점의 클로저

  // 해결 방법 1: 함수형 업데이트
  setCount(prev => prev + 1);  // prev는 항상 최신값

  // 해결 방법 2: useRef
  const countRef = useRef(count);
  countRef.current = count;  // 매 렌더링마다 최신값 저장
  // setInterval 내에서 countRef.current 참조
}
```

---

### 1.3 프로토타입 체인 & `this` 바인딩

#### this 바인딩 4가지 규칙
```javascript
// 1. 기본 바인딩 — 일반 함수 호출
function greet() { console.log(this); }
greet();  // window (non-strict) / undefined (strict mode)

// 2. 암시적 바인딩 — 메서드 호출
const obj = { name: 'Kyungil', greet() { console.log(this.name); } };
obj.greet();  // "Kyungil" — this = obj

// 3. 명시적 바인딩 — call, apply, bind
const fn = obj.greet;
fn.call(obj);     // "Kyungil" — this를 obj로 지정
fn.apply(obj);    // "Kyungil" — call과 같지만 인자를 배열로
const bound = fn.bind(obj);
bound();          // "Kyungil" — 영구 바인딩된 새 함수 반환

// 4. 화살표 함수 — 렉시컬 this (정의 시점의 외부 this를 상속)
const obj2 = {
  name: 'Kyungil',
  greet: () => console.log(this.name),  // this = 외부 스코프 (window/module)
  delayGreet() {
    setTimeout(() => {
      console.log(this.name);  // this = obj2 (delayGreet의 this를 상속)
    }, 100);
  }
};
```

#### 프로토타입 체인
```javascript
const animal = { speak() { return `${this.name} makes a sound`; } };
const dog = Object.create(animal);  // dog.__proto__ === animal
dog.name = 'Rex';
dog.speak();  // "Rex makes a sound" — animal에서 찾음

// 프로퍼티 조회 순서: dog 자체 → dog.__proto__ (animal) → animal.__proto__ (Object.prototype) → null
```

---

### 1.4 비동기 패턴 심화

```javascript
// === Promise 정적 메서드 비교 ===

// Promise.all — 전부 성공해야. 하나라도 reject되면 즉시 reject
const [a, b] = await Promise.all([fetchA(), fetchB()]);

// Promise.allSettled — 결과와 상관없이 전부 완료될 때까지 대기
const results = await Promise.allSettled([fetchA(), fetchB()]);
// results: [{ status: 'fulfilled', value: ... }, { status: 'rejected', reason: ... }]

// Promise.race — 가장 먼저 완료(성공이든 실패든)되는 것
const fastest = await Promise.race([fetchA(), timeout(3000)]);

// Promise.any — 가장 먼저 성공하는 것. 전부 실패하면 AggregateError
const first = await Promise.any([mirror1(), mirror2(), mirror3()]);

// === AbortController — fetch 취소 ===
const controller = new AbortController();
setTimeout(() => controller.abort(), 5000);  // 5초 후 취소
const res = await fetch('/api/data', { signal: controller.signal });

// === for await...of — 비동기 이터레이터 ===
async function* generateNumbers() {
  for (let i = 0; i < 5; i++) {
    await delay(100);
    yield i;
  }
}
for await (const num of generateNumbers()) {
  console.log(num);  // 0, 1, 2, 3, 4 (각 100ms 간격)
}
```

---

### 1.5 ES2022+ 핵심 문법

| 문법 | 설명 | 예시 |
|---|---|---|
| `?.` Optional chaining | null/undefined 안전 접근 | `user?.address?.zip` |
| `??` Nullish coalescing | null/undefined일 때만 기본값 | `value ?? 'default'` (`0`, `''` 통과) |
| `??=` Nullish assignment | null/undefined일 때만 할당 | `obj.x ??= 'default'` |
| `structuredClone()` | 깊은 복사 (순환참조 OK) | `const copy = structuredClone(obj)` |
| `Array.at(-1)` | 음수 인덱스 접근 | `[1,2,3].at(-1)` → 3 |
| `Object.hasOwn()` | 프로토타입 오염 안전 | `Object.hasOwn(obj, 'key')` |
| `#privateField` | 진짜 private 필드 | `class C { #x = 0; }` |
| Top-level `await` | 모듈 최상위 await | `const data = await fetch(...)` |
| `Object.groupBy()` | 배열 그룹핑 (ES2024) | `Object.groupBy(arr, item => item.type)` |

**`||` vs `??` 차이 (면접 단골):**
```javascript
0 || 'default'      // 'default' — 0은 falsy
0 ?? 'default'      // 0        — 0은 null/undefined가 아님
'' || 'default'     // 'default' — ''는 falsy
'' ?? 'default'     // ''        — ''은 null/undefined가 아님
null ?? 'default'   // 'default' — null은 nullish
```

---

## Part 2. TypeScript 핵심 (시니어 레벨)

### 2.1 타입 시스템 기초

```typescript
// === Union & Intersection ===
type StringOrNumber = string | number;         // 둘 중 하나
type Named = { name: string };
type Aged = { age: number };
type Person = Named & Aged;                    // 둘 다 필요: { name: string; age: number }

// === Literal Types ===
type HttpMethod = 'GET' | 'POST' | 'PUT' | 'DELETE';
type StatusCode = 200 | 201 | 400 | 404 | 500;

// === Type Narrowing (타입 좁히기) ===
function process(value: string | number) {
  if (typeof value === 'string') {
    return value.toUpperCase();    // TS가 string으로 좁혀줌
  }
  return value.toFixed(2);         // TS가 number로 좁혀줌
}

// instanceof narrowing
function handleError(err: Error | string) {
  if (err instanceof Error) {
    console.log(err.message);      // Error 타입
  } else {
    console.log(err);              // string 타입
  }
}
```

### 2.2 Discriminated Union (태그드 유니온) 상세

```typescript
// === 기본 패턴 ===
type ApiResponse<T> =
  | { status: 'success'; data: T; timestamp: number }
  | { status: 'error'; error: { code: number; message: string } }
  | { status: 'loading' };

function handleResponse(res: ApiResponse<User>) {
  switch (res.status) {
    case 'success':
      // TS가 data, timestamp 존재를 보장
      console.log(res.data.name, res.timestamp);
      break;
    case 'error':
      // TS가 error 존재를 보장
      console.log(res.error.message);
      break;
    case 'loading':
      // res에 data도 error도 없음
      break;
  }
}

// === Exhaustive Check (완전성 검사) ===
function assertNever(x: never): never {
  throw new Error(`Unexpected value: ${x}`);
}

function getStatusMessage(res: ApiResponse<User>): string {
  switch (res.status) {
    case 'success': return 'OK';
    case 'error':   return res.error.message;
    case 'loading': return 'Loading...';
    default:        return assertNever(res);
    // ↑ 새로운 status를 추가하면 여기서 컴파일 에러 발생!
    //   "Argument of type '{ status: "new_status"; ... }' is not assignable to never"
  }
}
```

**실전 활용 — React 이벤트 리듀서:**
```typescript
type Action =
  | { type: 'INCREMENT'; payload: number }
  | { type: 'DECREMENT'; payload: number }
  | { type: 'RESET' };

function reducer(state: number, action: Action): number {
  switch (action.type) {
    case 'INCREMENT': return state + action.payload;
    case 'DECREMENT': return state - action.payload;
    case 'RESET':     return 0;
    default:          return assertNever(action);
  }
}
```

### 2.3 Generic & Utility Types 심화

```typescript
// === Conditional Types ===
type IsString<T> = T extends string ? true : false;
type R1 = IsString<'hello'>;  // true
type R2 = IsString<42>;       // false

// infer — 타입 내부에서 추론
type ReturnType<T> = T extends (...args: any[]) => infer R ? R : never;
type UnpackPromise<T> = T extends Promise<infer U> ? U : T;
type T1 = UnpackPromise<Promise<string>>;  // string
type T2 = UnpackPromise<number>;           // number

// 실전: API 응답 타입에서 data만 추출
type ExtractData<T> = T extends { data: infer D } ? D : never;
type UserData = ExtractData<{ data: User; meta: Meta }>;  // User

// === Mapped Types ===
type Readonly<T> = { readonly [K in keyof T]: T[K] };
type Partial<T> = { [K in keyof T]?: T[K] };
type Required<T> = { [K in keyof T]-?: T[K] };  // -?로 optional 제거

// 실전: 모든 프로퍼티를 nullable로
type Nullable<T> = { [K in keyof T]: T[K] | null };

// Key remapping (TS 4.1+)
type Getters<T> = {
  [K in keyof T as `get${Capitalize<string & K>}`]: () => T[K];
};
type UserGetters = Getters<{ name: string; age: number }>;
// { getName: () => string; getAge: () => number }

// === Template Literal Types ===
type EventName<T extends string> = `on${Capitalize<T>}`;
type ClickEvent = EventName<'click'>;        // 'onClick'
type ChangeEvent = EventName<'change'>;      // 'onChange'

// 실전: CSS 속성 자동 생성
type CSSProperty = 'margin' | 'padding';
type CSSDirection = 'Top' | 'Right' | 'Bottom' | 'Left';
type CSSSpacing = `${CSSProperty}${CSSDirection}`;
// 'marginTop' | 'marginRight' | ... | 'paddingLeft' (8개 조합)
```

### 2.4 Branded Types (Nominal Typing)

```typescript
// TS는 구조적 타이핑이라 같은 shape이면 호환됨
// → Branded Type으로 "의미적으로 다른 타입"을 구분

type UserId = string & { readonly __brand: unique symbol };
type PostId = string & { readonly __brand: unique symbol };

// 생성 함수 (팩토리)
function createUserId(id: string): UserId { return id as UserId; }
function createPostId(id: string): PostId { return id as PostId; }

// 사용
function fetchUser(id: UserId): Promise<User> { /* ... */ }

const userId = createUserId('abc123');
const postId = createPostId('post456');

fetchUser(userId);   // ✅ OK
fetchUser(postId);   // ❌ 컴파일 에러! PostId ≠ UserId
fetchUser('raw');    // ❌ 컴파일 에러! string ≠ UserId
```

### 2.5 `unknown` vs `any` (면접 필수)

```typescript
// === any: 타입 체크 완전 무시 ===
let a: any = 'hello';
a.nonExistent();       // 런타임 에러, 컴파일타임 에러 없음
a.toFixed(2);          // 런타임 에러, 컴파일타임 에러 없음
let num: number = a;   // OK — 어디든 할당 가능

// === unknown: 타입 안전한 any ===
let u: unknown = 'hello';
u.toUpperCase();       // ❌ 컴파일 에러! 타입 가드 필요
let num2: number = u;  // ❌ 컴파일 에러!

// 사용하려면 반드시 타입을 좁혀야 함
if (typeof u === 'string') {
  u.toUpperCase();     // ✅ OK — string으로 좁혀짐
}

// 원칙: any 대신 unknown을 쓰고, 타입 가드로 좁혀서 사용
```

### 2.6 Type Guard 패턴 모음

```typescript
// 1. typeof — 원시 타입
function isString(x: unknown): x is string {
  return typeof x === 'string';
}

// 2. instanceof — 클래스 인스턴스
function isDate(x: unknown): x is Date {
  return x instanceof Date;
}

// 3. in — 객체 프로퍼티 존재 여부
function isFish(pet: Fish | Bird): pet is Fish {
  return 'swim' in pet;
}

// 4. 사용자 정의 Type Guard (가장 강력)
interface ApiError {
  code: number;
  message: string;
}
function isApiError(err: unknown): err is ApiError {
  return (
    typeof err === 'object' &&
    err !== null &&
    'code' in err &&
    'message' in err &&
    typeof (err as ApiError).code === 'number'
  );
}

// 5. Zod — 런타임 + 컴파일타임 동시 보장
import { z } from 'zod';
const UserSchema = z.object({
  id: z.string(),
  name: z.string(),
  age: z.number().min(0),
});
type User = z.infer<typeof UserSchema>;

const result = UserSchema.safeParse(apiResponse);
if (result.success) {
  result.data;   // User 타입으로 확정됨
} else {
  result.error;  // ZodError
}
```

---

## Part 3. React 핵심 (시니어 레벨)

### 3.1 렌더링 사이클 — 완전 분해

```
State/Props 변경 또는 부모 리렌더링
    ↓
╔══════════════════════════════════════════════╗
║ 1. RENDER PHASE (순수, 부수효과 없음)          ║
║    - 컴포넌트 함수 호출 (JSX → React Element) ║
║    - 재귀적으로 자식 컴포넌트도 호출            ║
║    - Virtual DOM(React Element 트리) 생성      ║
║    - Reconciliation: 이전 트리 vs 새 트리 비교 ║
║    ★ Concurrent Mode에서는 중단/재개 가능       ║
╚══════════════════════════════════════════════╝
    ↓
╔══════════════════════════════════════════════╗
║ 2. COMMIT PHASE (부수효과 실행)               ║
║    ① DOM 변경사항 적용 (실제 DOM 조작)         ║
║    ② useLayoutEffect 콜백 실행 (동기)         ║
║    ③ 브라우저 페인트 (사용자가 화면 변경을 봄)  ║
║    ④ useEffect 콜백 실행 (비동기)             ║
╚══════════════════════════════════════════════╝
```

**시니어 포인트 — Virtual DOM 오해 바로잡기:**
- Virtual DOM이 "항상 빠르다"는 것은 **틀림**. 직접 DOM 조작이 더 빠른 경우도 많음.
- Virtual DOM의 진짜 가치는 **선언적 프로그래밍 모델**을 제공하면서도 "충분히 빠른" 성능을 보장하는 것
- Reconciliation(Diffing) 알고리즘 시간복잡도: O(n) — 휴리스틱 기반 (동일 타입 = 같은 노드 가정)
- Svelte처럼 Virtual DOM 없이 직접 DOM을 갱신하는 프레임워크가 더 빠를 수 있음

### 3.2 Hooks 전체 정리

#### 상태 관련 Hooks
```typescript
// useState — 단순 상태
const [count, setCount] = useState(0);
setCount(prev => prev + 1);  // 함수형 업데이트: 항상 최신값 기반

// useReducer — 복잡한 상태 로직 (여러 액션, 중첩 객체)
const [state, dispatch] = useReducer(reducer, initialState);
dispatch({ type: 'INCREMENT', payload: 5 });

// useState vs useReducer 선택 기준:
// - 단순 값 토글, 카운터 → useState
// - 여러 sub-value, 복잡한 전이 로직, 다음 상태가 이전 상태에 의존 → useReducer
```

#### 참조 관련 Hooks
```typescript
// useRef — 렌더링에 영향 없는 가변 값 보관
const renderCount = useRef(0);
renderCount.current++;    // 변경해도 리렌더링 안 됨

const inputRef = useRef<HTMLInputElement>(null);
inputRef.current?.focus();  // DOM 요소 직접 접근

// useImperativeHandle — 부모에게 제한적 인터페이스 노출
useImperativeHandle(ref, () => ({
  focus: () => inputRef.current?.focus(),
  clear: () => { if (inputRef.current) inputRef.current.value = ''; },
}));
```

#### 부수효과 Hooks
```typescript
// useEffect — 비동기 부수효과 (렌더링 후 비동기 실행)
useEffect(() => {
  const subscription = eventBus.subscribe(handler);
  return () => subscription.unsubscribe();  // cleanup: 언마운트 시 또는 의존성 변경 시
}, [handler]);  // handler가 바뀔 때만 재실행

// useLayoutEffect — 동기 부수효과 (DOM 변경 직후, 페인트 전에 실행)
useLayoutEffect(() => {
  // DOM 측정(getBoundingClientRect 등) 후 즉시 상태 변경이 필요할 때
  // → 사용자에게 깜빡임 없이 반영
  const { height } = ref.current.getBoundingClientRect();
  setHeight(height);
}, []);

// useEffect cleanup 실행 타이밍:
// 1. 컴포넌트 언마운트 시
// 2. 의존성 배열의 값이 변경되어 effect가 재실행되기 직전
// 3. StrictMode에서 개발 환경: mount → cleanup → mount (의도적 2회 실행)
```

#### 성능 관련 Hooks
```typescript
// useMemo — 비싼 계산 결과 캐싱
const sorted = useMemo(
  () => items.sort((a, b) => a.price - b.price),
  [items]  // items가 바뀔 때만 재계산
);

// useCallback — 함수 참조 안정화
const handleClick = useCallback(
  (id: string) => { dispatch({ type: 'SELECT', id }); },
  [dispatch]  // dispatch는 안정적이므로 사실상 한 번만 생성
);

// useTransition — 비긴급 업데이트 표시 (React 18+)
const [isPending, startTransition] = useTransition();
const handleSearch = (query: string) => {
  setQuery(query);           // 긴급: 입력 필드 즉시 업데이트
  startTransition(() => {
    setResults(search(query)); // 비긴급: 무거운 검색 결과는 양보하며 처리
  });
};

// useDeferredValue — 값의 업데이트를 지연
const deferredQuery = useDeferredValue(query);
// query가 빠르게 바뀌어도 deferredQuery는 느리게 따라감
// → 디바운싱 대체 가능
```

#### 외부 연동 Hooks
```typescript
// useSyncExternalStore — 외부 store 구독 (React 18+)
// Zustand, Redux 등이 내부적으로 사용
const snapshot = useSyncExternalStore(
  store.subscribe,     // 구독 함수
  store.getSnapshot,   // 현재 상태 가져오기
  store.getServerSnapshot  // SSR용 (optional)
);

// useId — SSR에서도 안정적인 고유 ID 생성
const id = useId();
<label htmlFor={id}>Name</label>
<input id={id} />
```

### 3.3 메모이제이션 전략 상세

```
리렌더링이 발생하는 조건:
1. state가 변경됨
2. 부모가 리렌더링됨 (← React.memo로 방지 가능)
3. context 값이 변경됨 (← React.memo를 관통!)

React.memo가 무력화되는 경우:
- props로 매번 새로운 객체/배열/함수를 전달: { style: { color: 'red' } }
  → useMemo/useCallback으로 참조 안정화 필요
- context를 구독하는 경우: context 값이 바뀌면 memo를 무시하고 리렌더링
  → context 분리(작은 단위로 쪼개기) 또는 useMemo로 context value 안정화
```

> [!TIP]
> **React Compiler 1.0 (2025.10.7 안정 릴리스)** 이 컴포넌트 데이터 흐름을 분석하여 자동 메모이제이션을 처리. 수동 useMemo/useCallback/React.memo 필요성이 크게 감소. React 17+에서 사용 가능하며 Next.js, Vite, Expo 공식 템플릿에 통합됨. 하지만 **원리를 아는 것**이 면접에서 중요.

### 3.4 Concurrent Features 상세 (React 18+)

```typescript
// === Automatic Batching (자동 배칭) ===
// React 17: 이벤트 핸들러 내에서만 배칭
// React 18+: setTimeout, Promise, 네이티브 이벤트 핸들러에서도 배칭!
setTimeout(() => {
  setCount(1);
  setFlag(true);
  // React 18+: 리렌더링 1회 (배칭됨)
  // React 17: 리렌더링 2회 (배칭 안 됨)
}, 0);

// 배칭을 강제로 해제하고 싶다면:
import { flushSync } from 'react-dom';
flushSync(() => setCount(1));   // 여기서 즉시 리렌더링
flushSync(() => setFlag(true)); // 여기서 즉시 리렌더링

// === Suspense ===
const LazyComponent = React.lazy(() => import('./HeavyComponent'));
<Suspense fallback={<Spinner />}>
  <LazyComponent />   {/* 로드될 때까지 Spinner 표시 */}
</Suspense>

// React Query + Suspense 연동
const { data } = useSuspenseQuery({ queryKey: ['user'], queryFn: fetchUser });
// data가 undefined일 수 없음!  loading 상태는 상위 Suspense boundary가 처리
```

---

## Part 4. React vs React Native 렌더링 차이 (핵심!)

> [!IMPORTANT]
> 면접에서 "React를 아는데 RN은 뭐가 다르냐?"는 변형 질문이 자주 나옴

### 4.1 렌더링 파이프라인 비교

```
=== React (웹) ===
JSX → React.createElement() → Virtual DOM (JS 객체)
    ↓ Reconciliation (Diffing)
실제 DOM 조작 (document.createElement, appendChild 등)
    ↓ 브라우저 렌더링 엔진
Layout → Paint → Composite → 화면에 표시


=== React Native ===
JSX → React.createElement() → Virtual DOM (JS 객체)
    ↓ Reconciliation (Diffing)
    ↓ Fabric Renderer (C++)
Shadow Tree 생성/업데이트 (Yoga 레이아웃 엔진)
    ↓ 네이티브 뷰 명령
iOS: UIView / Android: android.view.View 생성/업데이트
    ↓ OS 렌더링 엔진
화면에 표시
```

### 4.2 핵심 차이점 상세

| 구분 | React (웹) | React Native |
|---|---|---|
| **렌더 타겟** | DOM 노드 (`<div>`, `<span>`) | 네이티브 뷰 (`UIView`, `android.view.View`) |
| **레이아웃 엔진** | 브라우저 CSS 엔진 | **Yoga** (C++ Flexbox 엔진) |
| **스타일링** | CSS (cascading, 상속) | **JS 객체** (`StyleSheet.create`), 상속 없음 (Text 제외) |
| **스레딩** | 싱글 스레드 (Main Thread) | **멀티 스레드** (JS, UI, Shadow, NativeModules) |
| **이벤트 처리** | 브라우저 이벤트 (click, input) | **Gesture Responder System** + Platform 터치 |
| **애니메이션** | CSS transition/animation, rAF | `Animated` API, **Reanimated** (UI Thread Worklet) |
| **리스트 가상화** | 직접 구현 or windowing 라이브러리 | **FlatList/SectionList** 내장 + FlashList |
| **텍스트 렌더링** | `<span>/<p>` 어디든 텍스트 가능 | **반드시 `<Text>` 컴포넌트 안에** |
| **컴포넌트 매핑** | `<div>` → DOM div | `<View>` → UIView / android.view.ViewGroup |

### 4.3 RN만의 추가 고려사항

#### 스레딩과 Bridge/JSI 통신
```
                    웹 React                          React Native
┌─────────────────────────────┐  ┌──────────────────────────────────────┐
│   Main Thread (유일)         │  │  JS Thread      ← 비즈니스 로직      │
│   - JS 실행                  │  │  UI Thread      ← 네이티브 UI 렌더링  │
│   - DOM 조작                 │  │  Background Thread ← 레이아웃 계산   │
│   - 레이아웃 계산             │  │  NativeModules  ← 비동기 네이티브 작업 │
│   → JS가 블로킹하면 UI 멈춤    │  │  → JS가 블로킹해도 UI는 계속 반응     │
└─────────────────────────────┘  │    (단, JS 스레드 작업은 밀림)         │
                                 └──────────────────────────────────────┘
```

> [!NOTE]
> **Fabric에서 Shadow Thread의 변화:** 레거시 아키텍처에서는 고정된 "Shadow Thread"가 Yoga 레이아웃 계산 전담. Fabric에서는 Shadow Tree 생성이 JS Thread를 포함한 여러 스레드에서 유연하게 수행 가능. Mount(실제 네이티브 뷰 적용)는 여전히 UI Thread에서 동기 실행.

#### 스타일 차이
```typescript
// 웹: CSS는 cascading(상속), 미디어쿼리, pseudo class 지원
div { font-size: 16px; }  /* 모든 하위 요소에 상속 */
div:hover { color: red; }  /* pseudo class */

// RN: JS 객체, 상속 없음 (Text 안의 Text만 제한적 상속)
const styles = StyleSheet.create({
  container: {
    fontSize: 16,          // 자식에게 상속되지 않음!
    flexDirection: 'column', // 기본값이 웹과 다름 (웹은 'row')
  },
});
// RN의 단위: dp (density-independent pixels), px/em/rem/% 없음
// hover 없음 → Pressable의 onPressIn/onPressOut으로 대체
```

#### 애니메이션 차이
```typescript
// 웹: CSS로 대부분 해결
.box { transition: transform 300ms ease; }
.box:hover { transform: scale(1.1); }

// RN에서 같은 것: Reanimated 필요
const scale = useSharedValue(1);
const style = useAnimatedStyle(() => ({
  transform: [{ scale: withSpring(scale.value) }],
}));
// Reanimated의 핵심: 이 코드가 UI Thread에서 직접 실행됨 (worklet)
// → JS Thread를 거치지 않으므로 60fps 보장
```

---

## Part 5. React Native 아키텍처 심화

### 5.1 New Architecture (0.76+ 기본) — 4요소

```
┌──────────────────────────────────────────────────────┐
│                    아키텍처 전체도                      │
│                                                      │
│  ┌───────────┐    JSI (C++)    ┌──────────────────┐  │
│  │ JS Thread │ ←─────────── → │  Native Threads  │  │
│  │ (Hermes)  │ 직접 참조,동기  │   UI / Shadow    │  │
│  └───────────┘                └──────────────────┘  │
│       │                              │               │
│  TurboModules                   Fabric Renderer      │
│  (lazy load)                   (동기 레이아웃)         │
│       │                              │               │
│  Codegen                        View Flattening      │
│  (TS spec → C++ 자동생성)        (불필요 뷰 제거)      │
│                                                      │
│  ★ Bridgeless Mode: 레거시 Bridge 인프라 완전 제거     │
└──────────────────────────────────────────────────────┘
```

| 컴포넌트 | 역할 | 이전과 비교 |
|---|---|---|
| **Fabric** | 새 렌더러. C++ Shadow Tree 공유, 동기 레이아웃, View Flattening | 이전: 비동기 레이아웃, 뷰 계층 그대로 |
| **TurboModules** | 네이티브 모듈 lazy loading + JSI 직접 호출 | 이전: 앱 시작 시 전체 초기화 + JSON 직렬화 |
| **JSI** | C++ 추상화 레이어, JSON 직렬화 제거, 동기 호출 가능 | 이전: Bridge를 통한 비동기 JSON 메시지 |
| **Codegen** | TS/Flow spec → C++ glue code 빌드타임 자동생성 | 이전: 런타임 타입 체크 (느리고 불안정) |

### 5.2 Hermes 엔진 상세

```
=== 빌드 시점 ===
JS 소스 코드 (.js/.ts)
    ↓ Hermes 컴파일러
Hermes Bytecode (.hbc)
    → 앱 번들에 포함

=== 런타임 ===
.hbc 파일 로드 → 바로 실행 (파싱 불필요!)

vs JavaScriptCore:
.js 파일 로드 → 파싱 → AST 생성 → 인터프리트/JIT 컴파일 → 실행
```

| 비교 | JavaScriptCore | Hermes |
|---|---|---|
| 컴파일 방식 | JIT (Just-In-Time) | AOT (Ahead-Of-Time) 바이트코드, **JIT 없음** |
| 앱 시작 시간 | 느림 (런타임 파싱+컴파일) | **빠름** (미리 컴파일된 바이트코드 즉시 실행) |
| 피크 성능 | JIT 최적화로 잠재적으로 높음 | JIT 없이 인터프리터로 실행, 모바일에 충분 |
| 메모리 | 높음 (JIT 코드 캐시 등) | **낮음** (바이트코드만 로드, 메모리 매핑 가능) |
| 디버깅 | Safari/Chrome DevTools | Chrome DevTools + **소스맵 필요** |
| 가비지 컬렉터 | Generational | **Hades** (concurrent GC, UI 스레드 블로킹 최소화) |
| 바이트코드 호환 | N/A | 버전 간 **비호환** (.hbc 포맷이 버전마다 변경) |

> [!WARNING]
> Hermes 바이트코드(.hbc)는 버전 간 호환되지 않음. OTA 업데이트 시 디바이스의 Hermes 런타임 버전과 번들 컴파일에 사용된 Hermes 버전이 반드시 일치해야 함. `runtimeVersion` 관리 필수.

### 5.3 스레딩 모델 & Reanimated Worklet

```
일반 애니메이션 (Animated API, JS-driven):
  JS Thread → Bridge/JSI → UI Thread → 렌더
  문제: JS Thread가 바쁘면 프레임 드랍

Reanimated Worklet 애니메이션:
  UI Thread에서 직접 JS(worklet) 실행 → 렌더
  → JS Thread 관여 없이 60fps 보장!

어떻게 가능?
1. 빌드타임: Babel 플러그인이 'worklet' 지시어가 있는 함수를 감지하여
   함수 본문과 캡처한 변수를 직렬화 가능한 형태로 분리
2. 런타임: 분리된 worklet 코드를 UI Thread 위의 별도 JS 런타임(JSI context)에 전달
3. SharedValue(JSI 기반)로 JS Thread ↔ UI Thread 간 동기 메모리 공유
4. runOnUI() / runOnJS()로 스레드 간 명시적 통신
```

```typescript
import Animated, {
  useSharedValue,
  useAnimatedStyle,
  withSpring,
  withTiming,
} from 'react-native-reanimated';

function AnimatedBox() {
  const offset = useSharedValue(0);

  // 이 함수는 UI Thread에서 실행됨 (worklet)
  const animatedStyle = useAnimatedStyle(() => ({
    transform: [{ translateX: withSpring(offset.value) }],
  }));

  return (
    <Animated.View style={[styles.box, animatedStyle]}
      onTouchStart={() => { offset.value = offset.value === 0 ? 200 : 0; }}
    />
  );
}
```

### 5.4 TurboModule 작성 흐름

```
1. TypeScript Spec 작성
   ┌──────────────────────────────────────┐
   │ // NativeLocalStorage.ts             │
   │ import { TurboModule } from 'react-native'; │
   │ export interface Spec extends TurboModule { │
   │   getItem(key: string): string | null;      │
   │   setItem(key: string, value: string): void;│
   │ }                                    │
   │ export default TurboModuleRegistry   │
   │   .getEnforcing<Spec>('NativeLocalStorage');│
   └──────────────────────────────────────┘
              ↓
2. Codegen (빌드 시 자동 실행)
   TS Spec → C++ 타입 안전 인터페이스 생성
              ↓
3. 네이티브 구현
   ├── Android (Kotlin):
   │   @ReactMethod fun getItem(key: String): String?
   │   @ReactMethod fun setItem(key: String, value: String)
   │
   └── iOS (Swift/ObjC):
       @objc func getItem(_ key: String) -> String?
       @objc func setItem(_ key: String, value: String)
              ↓
4. JS에서 사용:
   import NativeLocalStorage from './NativeLocalStorage';
   const value = NativeLocalStorage.getItem('key');
   // ↑ JSI를 통해 JSON 직렬화 없이 직접 호출
```

### 5.5 성능 최적화 체크리스트 (실전)

```
■ 리스트
  □ FlatList → FlashList (RecyclerListView 기반 cell recycling)
  □ getItemLayout 제공 (고정 높이 아이템)
  □ React.memo + useCallback으로 renderItem 메모이제이션
  □ keyExtractor에 안정적 고유 키 (index 사용 금지)
  □ windowSize 조정 (기본 21 → 5~7)

■ 이미지
  □ react-native-fast-image (디스크/메모리 캐싱)
  □ 서버에서 적절한 크기의 이미지 제공 (CDN resize)
  □ placeholder 이미지로 레이아웃 shift 방지

■ 네비게이션
  □ react-native-screens (네이티브 스크린 컨테이너)
  □ react-freeze (비활성 스크린 리렌더링 방지)

■ 애니메이션 & 제스처
  □ react-native-reanimated (UI Thread Worklet)
  □ react-native-gesture-handler (네이티브 제스처)

■ 빌드 & 런타임
  □ Hermes 활성화 (AOT 바이트코드)
  □ Metro: inline requires (큰 모듈 지연 로드)
  □ console.log 프로덕션에서 제거 (babel-plugin-transform-remove-console)

■ 모니터링
  □ Flipper + Hermes Sampling Profiler
  □ React DevTools Profiler
  □ Datadog RUM / Firebase Performance
```

---

## Part 6. 상태 관리 패턴 비교

### Zustand (콴다 스택)

```typescript
import { create } from 'zustand';
import { persist } from 'zustand/middleware';

// 기본 store
const useAuthStore = create<AuthState>((set, get) => ({
  token: null,
  isLoggedIn: false,
  login: (token: string) => set({ token, isLoggedIn: true }),
  logout: () => set({ token: null, isLoggedIn: false }),
  // get()으로 현재 상태 읽기
  getAuthHeader: () => `Bearer ${get().token}`,
}));

// 셀렉터 패턴: isLoggedIn이 바뀔 때만 리렌더링
const isLoggedIn = useAuthStore(state => state.isLoggedIn);

// React 외부에서도 접근 가능
const token = useAuthStore.getState().token;  // 네이티브 모듈, 인터셉터 등에서
useAuthStore.subscribe(state => {              // 외부 구독
  console.log('Auth state changed:', state.isLoggedIn);
});

// persist 미들웨어로 AsyncStorage 영구화
const useSettingsStore = create(
  persist<SettingsState>(
    (set) => ({ theme: 'light', setTheme: (t) => set({ theme: t }) }),
    { name: 'settings-storage', getStorage: () => AsyncStorage }
  )
);
```

### React Query (콴다 스택)

```typescript
// 기본 쿼리
const { data, isLoading, error, refetch } = useQuery({
  queryKey: ['user', userId],
  queryFn: () => fetchUser(userId),
  staleTime: 5 * 60_000,     // 5분간 fresh (refetch 안 함)
  gcTime: 30 * 60_000,       // 30분간 캐시 유지 (v5에서 cacheTime → gcTime)
  retry: 3,                  // 실패 시 3회 재시도
  retryDelay: attempt => Math.min(1000 * 2 ** attempt, 30_000), // 지수 백오프
});

// Mutation + Optimistic Update
const mutation = useMutation({
  mutationFn: updateUser,
  onMutate: async (newUser) => {
    await queryClient.cancelQueries({ queryKey: ['user', newUser.id] });
    const previous = queryClient.getQueryData(['user', newUser.id]);
    queryClient.setQueryData(['user', newUser.id], newUser); // 낙관적 업데이트
    return { previous };
  },
  onError: (err, newUser, context) => {
    queryClient.setQueryData(['user', newUser.id], context.previous); // 롤백
  },
  onSettled: () => {
    queryClient.invalidateQueries({ queryKey: ['user'] }); // 서버 재동기화
  },
});

// queryKey factory 패턴 (타입 안전)
const userKeys = {
  all:     ['users'] as const,
  lists:   () => [...userKeys.all, 'list'] as const,
  list:    (filters: Filters) => [...userKeys.lists(), filters] as const,
  details: () => [...userKeys.all, 'detail'] as const,
  detail:  (id: string) => [...userKeys.details(), id] as const,
};
```

### 역할 분담 원칙

```
"이 데이터의 source of truth는 어디인가?"

서버가 원본 (서버 상태)        → React Query
├─ API 응답 데이터
├─ 사용자 프로필
├─ 게시물 목록
└─ 캐싱, 자동 refetch, 무효화

클라이언트가 원본 (클라이언트 상태) → Zustand
├─ UI 상태 (모달 열림/닫힘, 사이드바)
├─ 테마, 언어 설정
├─ 인증 토큰 (보안 저장소 연동)
└─ 폼 입력값 (복잡한 다단계 폼)
```

---

## Part 7. 빠른 셀프 테스트 (면접 전날용)

> 아래 질문에 **입으로** 답변할 수 있으면 기본기 준비 완료.

| # | 영역 | 질문 | 핵심 키워드 |
|---|---|---|---|
| 1 | JS | `setTimeout(fn, 0)` vs `Promise.resolve().then(fn)` 실행 순서? | 마이크로>매크로 |
| 2 | JS | 클로저란? React stale closure 시나리오와 해결법 | 렉시컬 환경, useRef, 함수형 업데이트 |
| 3 | JS | `||` vs `??` 차이? `0 ?? 'default'`의 결과는? | nullish vs falsy |
| 4 | TS | `unknown` vs `any` 차이? 각각 언제? | 타입 가드 필요 여부 |
| 5 | TS | Discriminated Union + Exhaustive check 방법 | `assertNever(x: never)` |
| 6 | TS | Branded Type이 필요한 이유 | 구조적 타이핑 한계 보완 |
| 7 | React | Virtual DOM이 "항상 빠르다"가 **틀린** 이유 | 선언적 모델 + 충분한 성능 |
| 8 | React | `useEffect` cleanup 실행 타이밍 3가지 | 언마운트, 의존성 변경 전, StrictMode |
| 9 | React | `useLayoutEffect` vs `useEffect` 차이 | 동기(paint 전) vs 비동기(paint 후) |
| 10 | React | 자동 배칭이 React 18에서 확장된 범위 | setTimeout, Promise 내에서도 배칭 |
| 11 | RN | React vs RN 렌더링 파이프라인 차이? | DOM vs 네이티브뷰, Yoga vs CSS 엔진 |
| 12 | RN | New Arch 4요소? | Fabric, TurboModules, JSI, **Codegen** |
| 13 | RN | Hermes가 JSC보다 시작이 빠른 이유 | AOT 바이트코드 (.hbc) |
| 14 | RN | Reanimated Worklet이 60fps 보장하는 원리 | UI Thread에서 직접 JS 실행 |
| 15 | RN | CodePush 현재 상태와 대안 | App Center 종료, EAS Update, Shorebird |
| 16 | JS | `var` vs `let` vs `const` 차이? 호이스팅? | TDZ, 함수 스코프 vs 블록 스코프 |
| 17 | JS | debounce vs throttle 차이? 구현? | 마지막 호출 기준 vs 일정 간격 |
| 18 | React | Error Boundary란? 왜 class 컴포넌트만? | getDerivedStateFromError, componentDidCatch |
| 19 | React | Context API의 리렌더링 문제와 해결법 | Provider value 변경 시 전체 구독자 리렌더 |
| 20 | React | key의 역할? index를 key로 쓰면 안 되는 이유? | Reconciliation 식별자, 상태 유지 보장 |
| 21 | RN | React Navigation 핵심 구조? | Stack/Tab/Drawer, 딥링크, 네비게이션 상태 |
| 22 | RN | FlatList vs FlashList 차이와 최적화? | windowSize, getItemLayout, cell recycling |
| 23 | RN | Deep Linking 구현 방법? | URI scheme, Universal/App Links, navigation 연동 |

---

## Part 8. 빠진 기초 보완 — JavaScript

### 8.1 var / let / const & 호이스팅 (Hoisting)

```javascript
// === 스코프 차이 ===
// var: 함수 스코프 (function-scoped)
// let, const: 블록 스코프 (block-scoped)

function example() {
  if (true) {
    var a = 1;     // 함수 전체에서 접근 가능
    let b = 2;     // 이 if 블록 안에서만
    const c = 3;   // 이 if 블록 안에서만
  }
  console.log(a);  // 1 ✅
  console.log(b);  // ReferenceError ❌
  console.log(c);  // ReferenceError ❌
}

// === 호이스팅 (Hoisting) ===
// JS 엔진이 코드 실행 전에 선언을 스코프 최상단으로 "끌어올리는" 동작

// var 호이스팅: 선언만 끌어올려짐, 값은 undefined
console.log(x);   // undefined (에러가 아님!)
var x = 10;
// 실제 동작: var x; → console.log(x); → x = 10;

// let/const 호이스팅: 선언은 끌어올려지지만 TDZ에 갇힘
console.log(y);   // ReferenceError: Cannot access 'y' before initialization
let y = 10;
// TDZ(Temporal Dead Zone): 선언 ~ 초기화 사이의 구간에서 접근 불가

// 함수 선언문 호이스팅: 전체가 끌어올려짐
greet();                 // "Hello" ✅
function greet() { console.log("Hello"); }

// 함수 표현식: 변수만 호이스팅
greet2();                // TypeError: greet2 is not a function
var greet2 = function() { console.log("Hello"); };
```

```
호이스팅 정리:
┌─────────────┬──────────┬─────────────┬────────────────┐
│ 선언 방식    │ 호이스팅  │ 초기화 전 접근│ 스코프          │
├─────────────┼──────────┼─────────────┼────────────────┤
│ var         │ ✅       │ undefined   │ 함수 스코프     │
│ let         │ ✅ (TDZ) │ ReferenceError │ 블록 스코프  │
│ const       │ ✅ (TDZ) │ ReferenceError │ 블록 스코프  │
│ function    │ ✅ 전체  │ 사용 가능    │ 함수 스코프     │
│ class       │ ✅ (TDZ) │ ReferenceError │ 블록 스코프  │
└─────────────┴──────────┴─────────────┴────────────────┘

★ "호이스팅 안 된다"가 아니라 "호이스팅 되지만 TDZ에 갇힌다"가 정확한 표현
```

**const의 함정 (면접 단골):**
```javascript
const obj = { name: 'Kyungil' };
obj.name = 'Changed';     // ✅ 프로퍼티 변경은 가능
obj = { name: 'New' };    // ❌ 재할당은 불가
// const는 "바인딩(변수-값 연결)"이 불변이지, 값 자체가 불변이 아님
// 진짜 불변 객체: Object.freeze(obj) — 단, 얕은(shallow) freeze만
```

### 8.2 Debounce & Throttle

```
사용자가 검색창에 "react" 타이핑 (r → re → rea → reac → react):

Debounce (300ms):
r────re────rea────reac────react──[300ms대기]──→ 🔥 API 호출 1회 ("react")
"마지막 입력 후 일정 시간 동안 추가 입력이 없으면 실행"

Throttle (300ms):
r──→🔥──re──rea──→🔥──reac──react──→🔥
"일정 간격마다 최대 1회 실행"
```

```javascript
// === Debounce 직접 구현 ===
function debounce<T extends (...args: any[]) => void>(fn: T, delay: number) {
  let timer: ReturnType<typeof setTimeout>;
  return (...args: Parameters<T>) => {
    clearTimeout(timer);                    // 이전 타이머 취소
    timer = setTimeout(() => fn(...args), delay); // 새 타이머 설정
  };
}

// 사용: 검색창 입력
const debouncedSearch = debounce((query: string) => {
  fetch(`/api/search?q=${query}`);
}, 300);
input.addEventListener('input', (e) => debouncedSearch(e.target.value));

// === Throttle 직접 구현 ===
function throttle<T extends (...args: any[]) => void>(fn: T, interval: number) {
  let lastTime = 0;
  return (...args: Parameters<T>) => {
    const now = Date.now();
    if (now - lastTime >= interval) {
      lastTime = now;
      fn(...args);
    }
  };
}

// 사용: 스크롤 이벤트
const throttledScroll = throttle(() => {
  console.log('scroll position:', window.scrollY);
}, 100);
window.addEventListener('scroll', throttledScroll);
```

| 구분 | Debounce | Throttle |
|---|---|---|
| **언제** | 마지막 이벤트 후 대기 | 일정 간격마다 |
| **적합한 곳** | 검색 입력, 자동 저장, 리사이즈 후 계산 | 스크롤 위치 추적, 드래그, 마우스 이동 |
| **React에서** | `useDeferredValue` 또는 직접 구현 | `useRef` + `requestAnimationFrame` |
| **RN에서** | 동일 | `InteractionManager.runAfterInteractions` 활용도 가능 |

### 8.3 Symbol & Iterator/Generator

```javascript
// === Symbol — 고유한 식별자 ===
const id1 = Symbol('id');
const id2 = Symbol('id');
id1 === id2;  // false! — 같은 description이어도 항상 고유

// 용도 1: 객체 프로퍼티 충돌 방지
const PRIORITY = Symbol('priority');
const task = { name: 'Deploy', [PRIORITY]: 'high' };
// JSON.stringify(task) → '{"name":"Deploy"}' — Symbol 키는 열거 안 됨

// 용도 2: Well-known Symbols
class Range {
  constructor(public start: number, public end: number) {}

  // Symbol.iterator를 구현하면 for...of 사용 가능
  [Symbol.iterator]() {
    let current = this.start;
    const end = this.end;
    return {
      next() {
        return current <= end
          ? { value: current++, done: false }
          : { done: true };
      }
    };
  }
}
for (const num of new Range(1, 5)) console.log(num); // 1, 2, 3, 4, 5
[...new Range(1, 3)]; // [1, 2, 3] — 스프레드도 가능!

// === Generator — yield로 값을 하나씩 생성 ===
function* fibonacci() {
  let [a, b] = [0, 1];
  while (true) {
    yield a;           // 여기서 일시 정지, 값 반환
    [a, b] = [b, a + b];
  }
}
const fib = fibonacci();
fib.next(); // { value: 0, done: false }
fib.next(); // { value: 1, done: false }
fib.next(); // { value: 1, done: false }
fib.next(); // { value: 2, done: false }
// 무한 시퀀스도 메모리 걱정 없이 표현 가능 (lazy evaluation)
```

### 8.4 WeakMap / WeakSet / WeakRef

```javascript
// === WeakMap — key가 GC 대상이 되면 자동 제거 ===
const cache = new WeakMap();

function getMetadata(element) {
  if (cache.has(element)) return cache.get(element);
  const meta = computeExpensiveMetadata(element);
  cache.set(element, meta);  // element가 GC되면 이 엔트리도 자동 삭제
  return meta;
}

// vs Map: key가 GC되어도 엔트리가 남아서 메모리 누수

// WeakMap의 제약:
// - key는 반드시 객체 (원시값 불가)
// - 열거 불가 (keys(), values(), forEach 없음)
// - size 프로퍼티 없음

// === WeakRef — 약한 참조 (ES2022) ===
let target = { data: 'important' };
const weakRef = new WeakRef(target);

target = null;  // 강한 참조 제거
// 이후 GC가 실행되면...
weakRef.deref();  // undefined가 될 수 있음!

// 실전 활용: 캐시에서 GC 가능한 값 보관
// FinalizationRegistry와 함께 사용하여 정리 로직 등록
```

### 8.5 Proxy & Reflect

```javascript
// === Proxy — 객체 접근을 가로채기 ===
const handler = {
  get(target, prop, receiver) {
    console.log(`GET ${String(prop)}`);
    return Reflect.get(target, prop, receiver);
  },
  set(target, prop, value, receiver) {
    console.log(`SET ${String(prop)} = ${value}`);
    // 유효성 검사 가능
    if (prop === 'age' && typeof value !== 'number') {
      throw new TypeError('age must be a number');
    }
    return Reflect.set(target, prop, value, receiver);
  },
};

const user = new Proxy({ name: 'Kyungil', age: 30 }, handler);
user.name;       // LOG: "GET name"
user.age = 31;   // LOG: "SET age = 31"
user.age = '31'; // TypeError: age must be a number

// 실전 활용:
// 1. Vue 3의 반응성 시스템 (reactive())
// 2. MobX의 observable  
// 3. API 요청 로깅
// 4. 불변 객체 구현
// 5. 지연 로딩 (프로퍼티 접근 시 초기화)

// === Reflect — Proxy trap의 기본 동작 ===
// Reflect.get(), Reflect.set() 등은 Object의 동작을
// 함수형으로 제공하여 Proxy handler에서 "기본 동작 위임"에 사용
```

---

## Part 9. 빠진 기초 보완 — React

### 9.1 Error Boundary

```typescript
// Error Boundary는 class 컴포넌트로만 구현 가능
// 왜? getDerivedStateFromError/componentDidCatch가 class lifecycle에만 존재

class ErrorBoundary extends React.Component<
  { children: React.ReactNode; fallback: React.ReactNode },
  { hasError: boolean; error: Error | null }
> {
  state = { hasError: false, error: null };

  // 렌더링 중 에러 발생 시 호출 (render phase)
  static getDerivedStateFromError(error: Error) {
    return { hasError: true, error };
  }

  // 에러 정보 로깅 (commit phase)
  componentDidCatch(error: Error, errorInfo: React.ErrorInfo) {
    // Sentry, Datadog 등에 전송
    logErrorToService(error, errorInfo.componentStack);
  }

  render() {
    if (this.state.hasError) {
      return this.props.fallback; // 폴백 UI
    }
    return this.props.children;
  }
}

// 사용
<ErrorBoundary fallback={<ErrorPage />}>
  <App />
</ErrorBoundary>
```

**Error Boundary가 잡지 못하는 에러:**
- 이벤트 핸들러 (try-catch로 직접 처리)
- 비동기 코드 (setTimeout, Promise)
- 서버 사이드 렌더링
- Error Boundary 자체에서 발생한 에러

```typescript
// 실전: react-error-boundary 라이브러리 사용 (함수형 지원)
import { ErrorBoundary } from 'react-error-boundary';

<ErrorBoundary
  FallbackComponent={ErrorFallback}
  onReset={() => queryClient.resetQueries()}  // "다시 시도" 버튼 시 리셋
  resetKeys={[userId]}  // userId가 바뀌면 에러 상태 자동 리셋
>
  <UserProfile />
</ErrorBoundary>
```

### 9.2 Context API & 리렌더링 문제

```typescript
// === 기본 사용 ===
const ThemeContext = React.createContext<'light' | 'dark'>('light');

function App() {
  const [theme, setTheme] = useState<'light' | 'dark'>('light');
  return (
    <ThemeContext.Provider value={theme}>
      <Header />    {/* theme이 바뀌면 전부 리렌더링! */}
      <Content />
      <Footer />
    </ThemeContext.Provider>
  );
}

function Header() {
  const theme = useContext(ThemeContext);  // 이 컴포넌트"만" theme을 필요로 함
  return <header className={theme}>...</header>;
}
```

**Context 리렌더링 문제:**
```
Provider의 value가 바뀌면 → useContext를 쓰는 모든 컴포넌트가 리렌더링
React.memo로도 방지 불가!! (memo를 관통)

해결 방법:
1. Context 분리 — 자주 바뀌는 값과 안 바뀌는 값을 별도 Context로
2. useMemo로 value 안정화
3. 작은 상태 → Zustand/Jotai 사용 (selector로 구독 범위 제한)
```

```typescript
// 해결법 1: Context 분리
const ThemeContext = createContext('light');     // 거의 안 바뀜
const ThemeDispatchContext = createContext(null); // dispatch 함수 (stable)

// 해결법 2: useMemo
function App() {
  const [theme, setTheme] = useState('light');
  const value = useMemo(() => ({ theme, setTheme }), [theme]);
  return <ThemeContext.Provider value={value}>...</ThemeContext.Provider>;
}

// 해결법 3: Context는 정적 설정만, 동적 상태는 Zustand
// → 콴다의 접근 방식 (Zustand + React Query로 상태관리)
```

### 9.3 key prop의 역할

```typescript
// key는 React가 Reconciliation 시 "어떤 요소가 같은 요소인지" 식별하는 유일한 방법

// ❌ index를 key로 사용하면 안 되는 이유
const [items, setItems] = useState(['A', 'B', 'C']);
// 렌더: [A(0), B(1), C(2)]

// "A"를 삭제하면 → ['B', 'C']
// index key: [B(0), C(1)]
// React가 보는 것: key=0이 "A→B"로 바뀜, key=1이 "B→C"로 바뀜, key=2 삭제
// → 모든 요소를 업데이트! (비효율적)
// → 각 요소의 상태(input값 등)가 엉뚱한 항목에 남음!

// ✅ 고유 ID를 key로
items.map(item => <Item key={item.id} data={item} />);
// "A(id:1)" 삭제 → React가 key=1만 제거, key=2, key=3은 그대로 유지

// key를 이용한 상태 리셋 트릭:
// key가 바뀌면 React는 완전히 새로운 컴포넌트로 인식 → 상태 초기화
<UserForm key={userId} initialData={userData} />
// userId가 바뀌면 폼이 완전히 리셋됨 (보나파이드 트릭)
```

### 9.4 forwardRef & useImperativeHandle

```typescript
// forwardRef: 부모가 자식 컴포넌트 내부의 DOM 요소에 ref를 전달할 때

// ❌ 일반 컴포넌트: ref는 props로 전달 불가
function Input(props) {
  return <input ref={props.ref} />;  // ref는 특수 prop, 전달 안 됨
}

// ✅ forwardRef 사용
const Input = forwardRef<HTMLInputElement, InputProps>((props, ref) => {
  return <input ref={ref} {...props} />;
});

// 부모에서:
const inputRef = useRef<HTMLInputElement>(null);
<Input ref={inputRef} />
inputRef.current?.focus();

// === useImperativeHandle: 노출할 메서드 제한 ===
const CustomInput = forwardRef<CustomInputRef, Props>((props, ref) => {
  const inputRef = useRef<HTMLInputElement>(null);

  useImperativeHandle(ref, () => ({
    // 전체 DOM을 노출하지 않고, 필요한 메서드만 제공
    focus: () => inputRef.current?.focus(),
    clear: () => { if (inputRef.current) inputRef.current.value = ''; },
    getValue: () => inputRef.current?.value ?? '',
  }));

  return <input ref={inputRef} {...props} />;
});

// React 19+ 변경: forwardRef 없이 ref를 일반 prop으로 전달 가능
// function Input({ ref, ...props }) { return <input ref={ref} {...props} /> }
```

### 9.5 Fiber 아키텍처

```
React Fiber = React 16에서 도입된 내부 reconciliation 엔진
핵심: 렌더링 작업을 "작은 단위(fiber)"로 분할하여 중단/재개 가능

이전 (Stack Reconciler):
  렌더링 시작 ────────────────────── 렌더링 완료
  [한 번에 전체 트리 처리, 중단 불가]
  → 큰 트리면 UI가 멈춤 (jank)

Fiber (Fiber Reconciler):
  렌더링 시작 ─[fiber1]─[양보]─[fiber2]─[양보]─[fiber3]─ 완료
  → 각 fiber 단위(=컴포넌트)에서 중단 가능
  → 중간에 사용자 입력 등 우선순위 높은 작업 처리

Fiber 노드 구조:
┌──────────────────────┐
│ Fiber Node           │
│ - type: 컴포넌트 타입 │
│ - key: reconciliation 키│
│ - child → 첫 번째 자식  │
│ - sibling → 다음 형제   │ ← 연결 리스트로 트리 순회
│ - return → 부모        │
│ - stateNode: 실제 인스턴스│
│ - pendingProps        │
│ - memoizedState: hooks │
│ - effectTag: 수행할 작업 │
│ - lanes: 우선순위       │ ← Concurrent Features의 기반
└──────────────────────┘
```

**시니어 포인트:**
- Fiber가 Concurrent Mode를 가능하게 만든 근본 구조
- `useTransition`, `Suspense` 모두 Fiber의 **작업 중단/재개** 능력 위에 동작
- 각 fiber에 **lanes(우선순위)**가 할당되어 긴급 업데이트(입력)가 비긴급(검색 결과)보다 먼저 처리됨

---

## Part 10. 빠진 기초 보완 — React Native

### 10.1 React Navigation (JD 명시!)

```typescript
// === 기본 구조 ===
import { NavigationContainer } from '@react-navigation/native';
import { createNativeStackNavigator } from '@react-navigation/native-stack';
import { createBottomTabNavigator } from '@react-navigation/bottom-tabs';

// 타입 정의 (TypeScript)
type RootStackParamList = {
  Home: undefined;
  Profile: { userId: string };
  Settings: { section?: string };
};

const Stack = createNativeStackNavigator<RootStackParamList>();
const Tab = createBottomTabNavigator();

function App() {
  return (
    <NavigationContainer>
      <Stack.Navigator initialRouteName="Home">
        <Stack.Screen name="Home" component={HomeScreen} />
        <Stack.Screen name="Profile" component={ProfileScreen} />
      </Stack.Navigator>
    </NavigationContainer>
  );
}

// === 네비게이션 사용 ===
// 1. hook 기반 (함수 컴포넌트)
function HomeScreen() {
  const navigation = useNavigation<NativeStackNavigationProp<RootStackParamList>>();
  const route = useRoute<RouteProp<RootStackParamList, 'Home'>>();

  return (
    <Button onPress={() => navigation.navigate('Profile', { userId: 'abc' })} />
  );
}

// 2. 네비게이션 메서드
navigation.navigate('Profile', { userId: '123' }); // 이미 스택에 있으면 이동
navigation.push('Profile', { userId: '456' });      // 항상 새 스크린 추가
navigation.goBack();                                 // 뒤로
navigation.reset({ index: 0, routes: [{ name: 'Home' }] }); // 스택 초기화
```

```
네비게이터 종류:
┌─────────────────────┬──────────────────────────────┐
│ Stack Navigator     │ 화면 쌓기 (push/pop)          │
│ Bottom Tab          │ 하단 탭 바                    │
│ Material Top Tab    │ 상단 탭 (swipeable)           │
│ Drawer              │ 사이드 메뉴                   │
│ Native Stack        │ 네이티브 스택 (더 좋은 성능)   │
└─────────────────────┴──────────────────────────────┘

★ react-native-screens: 네이티브 스크린 컨테이너를 사용하여
  비활성 스크린의 뷰를 네이티브 레벨에서 최적화
★ react-freeze: 비활성 탭의 리렌더링을 완전 방지
```

### 10.2 FlatList 상세 (RN 리스트의 핵심)

```typescript
<FlatList
  data={items}
  keyExtractor={(item) => item.id}  // 안정적 고유 키 (index 사용 금지!)

  // === 렌더링 ===
  renderItem={({ item, index }) => <ItemRow item={item} />}
  ItemSeparatorComponent={() => <View style={styles.separator} />}
  ListHeaderComponent={<Header />}
  ListFooterComponent={isLoading ? <Spinner /> : null}
  ListEmptyComponent={<EmptyState />}

  // === 성능 (면접에서 자주 물어봄) ===
  initialNumToRender={10}        // 초기 렌더링 아이템 수
  maxToRenderPerBatch={10}       // 배치당 렌더링 수
  windowSize={5}                 // 화면 밖 렌더링 범위 (기본 21 → 5~7 권장)
  removeClippedSubviews={true}   // 화면 밖 뷰 detach (Android에서 효과적)
  getItemLayout={(data, index) => ({  // 고정 높이일 때 레이아웃 계산 스킵!
    length: ITEM_HEIGHT,
    offset: ITEM_HEIGHT * index,
    index,
  })}

  // === 무한 스크롤 ===
  onEndReached={fetchNextPage}
  onEndReachedThreshold={0.5}    // 끝에서 50% 지점에서 트리거

  // === 새로고침 ===
  refreshing={isRefreshing}
  onRefresh={handleRefresh}
/>
```

```
FlatList vs FlashList 비교:
┌──────────────┬─────────────────────────────────────────┐
│ FlatList     │ VirtualizedList 기반                     │
│              │ 화면 밖 아이템 언마운트 → 재마운트          │
│              │ → 마운트 비용이 큼                        │
├──────────────┼─────────────────────────────────────────┤
│ FlashList    │ RecyclerListView 기반 (Shopify)          │
│ (권장)       │ 화면 밖 아이템 언마운트 안 함               │
│              │ → cell recycling (타입별 재활용)           │
│              │ → 마운트 0회, props만 업데이트              │
│              │ ★ estimatedItemSize 필수                  │
└──────────────┴─────────────────────────────────────────┘
```

### 10.3 Keyboard & TextInput 처리

```typescript
// RN에서 가장 흔한 이슈: 키보드가 입력 필드를 가림

// === 해결법 1: KeyboardAvoidingView ===
<KeyboardAvoidingView
  behavior={Platform.OS === 'ios' ? 'padding' : 'height'}
  keyboardVerticalOffset={Platform.OS === 'ios' ? 64 : 0}
>
  <TextInput ... />
  <Button title="Submit" />
</KeyboardAvoidingView>

// === 해결법 2: react-native-keyboard-aware-scroll-view ===
// ScrollView 안에 여러 TextInput이 있을 때 자동 스크롤

// === Keyboard API ===
import { Keyboard } from 'react-native';

// 키보드 이벤트 구독
useEffect(() => {
  const showSub = Keyboard.addListener('keyboardDidShow', (e) => {
    setKeyboardHeight(e.endCoordinates.height);
  });
  const hideSub = Keyboard.addListener('keyboardDidHide', () => {
    setKeyboardHeight(0);
  });
  return () => { showSub.remove(); hideSub.remove(); };
}, []);

// 키보드 닫기
Keyboard.dismiss();

// === TextInput 핵심 props ===
<TextInput
  value={text}
  onChangeText={setText}
  onSubmitEditing={() => nextInput.current?.focus()} // 다음 필드로 포커스
  returnKeyType="next"        // 키보드 리턴키 타입
  autoCapitalize="none"       // 자동 대문자 끄기
  autoCorrect={false}         // 자동 수정 끄기
  secureTextEntry={true}      // 비밀번호 마스킹
  keyboardType="email-address" // 키보드 타입
  blurOnSubmit={false}        // submit 시 포커스 유지
  ref={inputRef}
/>
```

### 10.4 Deep Linking

```typescript
// === URI Scheme (커스텀 스킴) ===
// qanda://profile/123 → 앱 내 프로필 페이지로 이동

// === Universal Links (iOS) / App Links (Android) ===
// https://qanda.ai/profile/123 → 앱 설치됨: 앱으로, 없으면: 웹으로

// React Navigation 연동:
const linking = {
  prefixes: ['qanda://', 'https://qanda.ai'],
  config: {
    screens: {
      Home: '',
      Profile: 'profile/:userId',
      Settings: 'settings',
      Question: {
        path: 'question/:questionId',
        parse: { questionId: (id: string) => parseInt(id) },
      },
    },
  },
};

// NavigationContainer에 전달
<NavigationContainer linking={linking} fallback={<LoadingScreen />}>
  <Stack.Navigator>...</Stack.Navigator>
</NavigationContainer>

// 앱이 이미 열려있을 때 딥링크 수신
import { Linking } from 'react-native';
useEffect(() => {
  const sub = Linking.addEventListener('url', ({ url }) => {
    // url 파싱 후 네비게이션
  });
  return () => sub.remove();
}, []);
```

### 10.5 SafeArea, Platform, Dimensions, StatusBar

```typescript
// === SafeAreaView: 노치, 홈 인디케이터 대응 ===
import { SafeAreaView } from 'react-native-safe-area-context';
// react-native 내장 SafeAreaView는 iOS만 지원 → 라이브러리 사용 권장

// useSafeAreaInsets: 정밀한 제어
const insets = useSafeAreaInsets();
<View style={{ paddingTop: insets.top, paddingBottom: insets.bottom }}>

// === Platform: OS별 분기 ===
import { Platform } from 'react-native';

const styles = StyleSheet.create({
  shadow: Platform.select({
    ios: {
      shadowColor: '#000',
      shadowOffset: { width: 0, height: 2 },
      shadowOpacity: 0.25,
    },
    android: {
      elevation: 5,
    },
  }),
});

// 플랫폼별 파일: Component.ios.tsx / Component.android.tsx
// RN이 자동으로 올바른 파일을 import

// === Dimensions ===
import { Dimensions, useWindowDimensions } from 'react-native';

// 정적 (회전 시 업데이트 안 됨)
const { width, height } = Dimensions.get('window');

// 동적 (회전 시 자동 업데이트, 권장)
function MyComponent() {
  const { width, height } = useWindowDimensions();
  const isLandscape = width > height;
}

// === StatusBar ===
import { StatusBar } from 'react-native';
<StatusBar barStyle="dark-content" backgroundColor="white" translucent={false} />
// iOS: barStyle만 적용
// Android: backgroundColor, translucent도 적용
```

### 10.6 AsyncStorage vs MMKV

```typescript
// === AsyncStorage: 기본 key-value 저장소 ===
import AsyncStorage from '@react-native-async-storage/async-storage';
await AsyncStorage.setItem('token', 'abc123');  // 비동기!
const token = await AsyncStorage.getItem('token');

// 특징:
// - 완전 비동기 (모든 호출에 await 필요)
// - 문자열만 저장 (객체는 JSON.stringify 필요)
// - 느림 (SQLite 기반, iOS: 파일 시스템)
// - 6MB 제한 (Android)

// === MMKV: 고성능 대안 ===
import { MMKV } from 'react-native-mmkv';
const storage = new MMKV();
storage.set('token', 'abc123');  // 동기!
const token = storage.getString('token');

// 특징:
// - 동기 (즉시 반환, await 불필요)
// - 30배 이상 빠름 (memory-mapped files)
// - 다양한 타입 직접 저장 (string, number, boolean, Buffer)
// - 암호화 지원
// - Zustand persist 연동 가능

// Zustand + MMKV 연동
import { StateStorage } from 'zustand/middleware';
const zustandStorage: StateStorage = {
  getItem: (name) => storage.getString(name) ?? null,
  setItem: (name, value) => storage.set(name, value),
  removeItem: (name) => storage.delete(name),
};
```

| 비교 | AsyncStorage | MMKV |
|---|---|---|
| **API** | 비동기 (async/await) | **동기** |
| **속도** | 느림 | **~30배 빠름** |
| **타입** | 문자열만 | string, number, boolean, Buffer |
| **암호화** | ❌ | ✅ 내장 |
| **크기 제한** | 6MB (Android) | 사실상 없음 |
| **적합한 용도** | 간단한 설정 | 토큰, 캐시, 상태 영구화 |

---

## Part 11. React Native vs Flutter 비교 (경험 살리기)

> [!IMPORTANT]
> Flutter 경험이 있으므로 면접에서 "Flutter와 비교해서 RN은 어떤가요?" 질문이 나올 확률 높음.
> 차이와 공통점을 정확히 알면 "두 프레임워크를 깊이 이해한다"는 인상을 줄 수 있음.

### 11.1 SafeArea / ViewPadding 개념 비교

```
Flutter:
┌──────────────────────────┐
│ MediaQuery               │
│  .padding     = SafeArea 전체 (top + bottom)
│  .viewPadding = 키보드 있어도 유지되는 패딩 (노치, 상태바)
│  .viewInsets  = 키보드 높이 (소프트 키보드가 차지하는 영역)
│                                                              
│  SafeArea 위젯: MediaQuery.padding을 자동 적용              
│  → 키보드가 올라오면 bottom padding이 0이 됨                  
│    (키보드가 이미 그 공간을 차지하므로)                        
│                                                              
│  viewPadding은 키보드와 무관하게 항상 값이 유지됨             
│  → 키보드 위에 버튼을 고정하고 싶을 때 유용                  
└──────────────────────────┘

React Native:
┌──────────────────────────┐
│ react-native-safe-area-context  │
│  useSafeAreaInsets()      │
│  → { top, bottom, left, right }
│  → 이 값은 하드웨어 기반 (노치, 홈 인디케이터)
│  → 키보드와 무관 (Flutter의 viewPadding에 해당)
│                                                              
│  키보드 높이: Keyboard API로 별도 관리                       
│  → Keyboard.addListener('keyboardDidShow')                   
│  → KeyboardAvoidingView로 레이아웃 조정                      
└──────────────────────────┘
```

| 개념 | Flutter | React Native |
|---|---|---|
| **노치/상태바 패딩** | `MediaQuery.of(context).padding` | `useSafeAreaInsets()` |
| **키보드 높이** | `MediaQuery.of(context).viewInsets.bottom` | `Keyboard.addListener` |
| **키보드 무관 패딩** | `MediaQuery.of(context).viewPadding` | `useSafeAreaInsets()` (항상 하드웨어 기반) |
| **SafeArea 위젯** | `SafeArea()` 위젯 | `<SafeAreaView>` 컴포넌트 |
| **키보드 회피** | `Scaffold`가 자동 처리 (`resizeToAvoidBottomInset`) | `<KeyboardAvoidingView>` 수동 설정 |

### 11.2 렌더링 아키텍처 비교

```
Flutter:
Widget Tree → Element Tree → RenderObject Tree
     ↓                              ↓
  불변 설정         Skia/Impeller로 직접 픽셀 렌더링
  (rebuild 저렴)    (OS 네이티브 뷰를 전혀 사용 안 함!)

React Native:
JSX → React Element Tree → Shadow Tree (Yoga) → 네이티브 뷰
     ↓ Reconciliation              ↓
  Virtual DOM Diff      OS의 네이티브 뷰 (UIView, android.view.View)
```

| 관점 | Flutter | React Native |
|---|---|---|
| **렌더링 방식** | **자체 엔진**(Skia/Impeller)으로 직접 그림 | OS의 **네이티브 뷰** 사용 |
| **네이티브 UI** | 모든 걸 직접 그림 (네이티브 룩 모방) | 실제 네이티브 컴포넌트 사용 |
| **레이아웃** | RenderObject의 자체 레이아웃 (RenderBox 등) | **Yoga** (C++ Flexbox) |
| **UI 일관성** | iOS/Android 100% 동일 | OS별 네이티브 차이 발생 가능 |
| **플랫폼 느낌** | 커스텀 필요 (Cupertino/Material) | 기본 제공 (실제 네이티브) |
| **JS 스레드** | 없음 (Dart VM이 직접 처리) | JS Thread가 비즈니스 로직 담당 |
| **Hot Reload** | Dart VM 내 (빠름) | Metro + Hermes (빠름) |

### 11.3 상태관리 비교

```
Flutter:
├── 내장: setState, InheritedWidget
├── Provider / Riverpod   ← InheritedWidget 래퍼
├── BLoC                  ← Stream 기반 (이벤트 → 상태)
├── GetX                  ← 반응형 (Rx)
└── flutter_hooks         ← React Hooks 영감

React Native:
├── 내장: useState, useReducer, Context
├── Zustand               ← 외부 store (콴다)
├── Jotai                 ← atom 기반 (drpalette)
├── Redux Toolkit         ← 전통적 Flux 패턴
├── React Query           ← 서버 상태 전용
└── MobX                  ← Observable (Proxy 기반)
```

| 비교 축 | Flutter | React Native |
|---|---|---|
| **상태 불변성** | setState에서 명시적 | `set(prev => ({...prev}))` 불변 업데이트 |
| **리빌드 범위** | `context.watch` / `Consumer` | `useSelector` / `useSelectAtom` |
| **서버 상태** | Riverpod `AsyncValue` / BLoC | **React Query** (전용 솔루션) |
| **DI** | Provider, get_it | Context, Module scope |

### 11.4 네비게이션 비교

| 관점 | Flutter | React Native |
|---|---|---|
| **기본 제공** | `Navigator 2.0` (선언적) | 없음 → **React Navigation** |
| **라우팅** | `go_router`, `auto_route` | `@react-navigation/native` |
| **딥링크** | `go_router`의 URL 패턴 매칭 | `linking` 설정 (URL → screen 매핑) |
| **타입 안전성** | `go_router` 타입 지원 | `ParamList` 제네릭으로 보장 |
| **스크린 전환** | `PageRouteBuilder`, `Hero` | `react-native-screens` + 커스텀 |
| **중첩 네비게이션** | `ShellRoute` (go_router) | `Tab > Stack` 중첩 조합 |
| **상태 보존** | `AutomaticKeepAliveClientMixin` | `react-freeze` + 네이티브 스크린 |

### 11.5 컴포넌트 / 위젯 1:1 매핑

| Flutter 위젯 | React Native 컴포넌트 | 비고 |
|---|---|---|
| `Container` | `View` | 기본 레이아웃 박스 |
| `Text` | `Text` | RN도 Text 안에만 텍스트 가능 |
| `Image.network()` | `Image` / `FastImage` | RN은 캐싱에 별도 라이브러리 |
| `ListView.builder` | `FlatList` / `FlashList` | 가상화 리스트 |
| `GestureDetector` | `Pressable` / `GestureHandler` | RN은 Gesture Handler 라이브러리 |
| `TextField` | `TextInput` | |
| `Scaffold` | 직접 구성 (`SafeArea` + `View`) | RN에 Scaffold 없음 |
| `AppBar` | `Stack.Screen options` | Navigation 헤더로 처리 |
| `BottomNavigationBar` | `createBottomTabNavigator` | |
| `Drawer` | `createDrawerNavigator` | |
| `Column` | `View` (flexDirection: 'column') | RN 기본이 column |
| `Row` | `View` (flexDirection: 'row') | |
| `Expanded` | `flex: 1` | |
| `SizedBox` | `View` (width/height 고정) | |
| `Padding` | style `padding` | RN은 별도 위젯 없음 |
| `SingleChildScrollView` | `ScrollView` | |
| `Stack` (overlay) | `View` + `position: 'absolute'` | |
| `showDialog` | `Modal` 컴포넌트 | |
| `AnimatedBuilder` | `Animated.View` / Reanimated | |
| `Provider` | `Context.Provider` / Zustand | |
| `MediaQuery` | `useWindowDimensions` + `useSafeAreaInsets` | |
| `Theme.of(context)` | `useColorScheme()` / Context 기반 테마 | |
| `Navigator.push` | `navigation.navigate` / `push` | |

### 11.6 공통점 (면접에서 어필)

```
1. 선언적 UI
   Flutter: Widget build() { return Column(children: [...]) }
   RN:      function App() { return <View><Text>...</Text></View> }
   → 둘 다 "UI = f(state)" 패턴

2. Hot Reload
   Flutter: Dart VM 내 직접 코드 교체
   RN:      Metro bundler + Fast Refresh
   → 둘 다 상태 유지하면서 실시간 반영

3. 단일 코드베이스 → iOS + Android
   크로스 플랫폼의 핵심 가치 동일

4. 플랫폼 채널 / 네이티브 모듈
   Flutter: MethodChannel(비동기) / FFI(동기)
   RN:      Bridge(레거시, 비동기) / JSI + TurboModules(동기)
   → 둘 다 네이티브 코드(Swift/Kotlin)와 통신하는 브릿지 제공

5. 레이아웃 시스템: Flexbox 기반
   Flutter: Flex → Column/Row/Wrap
   RN:      Yoga 엔진 (C++ Flexbox)
   → API는 다르지만 개념은 동일 (main axis, cross axis, flex 비율)

6. OTA 업데이트
   Flutter: Shorebird (Dart 패치)
   RN:      EAS Update / CodePush (JS 번들 패치)
   → JS/Dart 레이어만 교체, 네이티브 바이너리 불변

7. 상태 불변성 원칙
   Flutter: setState(() { _count++; }) → 새 Widget 트리 빌드
   RN:      setCount(prev => prev + 1) → 새 React Element 트리 빌드
   → 둘 다 상태 변경 → UI 트리 재구성 → diff → 최소 업데이트
```

---

## Part 7. 빠른 셀프 테스트 (면접 전날용) — 확장

> 아래 질문에 **입으로** 답변할 수 있으면 기본기 준비 완료.

| # | 영역 | 질문 | 핵심 키워드 |
|---|---|---|---|
| 1 | JS | `setTimeout(fn, 0)` vs `Promise.resolve().then(fn)` 실행 순서? | 마이크로>매크로 |
| 2 | JS | 클로저란? React stale closure 시나리오와 해결법 | 렉시컬 환경, useRef, 함수형 업데이트 |
| 3 | JS | `\|\|` vs `??` 차이? `0 ?? 'default'`의 결과는? | nullish vs falsy |
| 4 | JS | `var` vs `let` vs `const` 차이? TDZ란? | 호이스팅, 블록 스코프, Temporal Dead Zone |
| 5 | JS | debounce vs throttle 차이? 직접 구현? | clearTimeout, Date.now() |
| 6 | JS | Proxy의 용도? Vue/MobX와의 관계? | get/set trap, 반응성 시스템 |
| 7 | TS | `unknown` vs `any` 차이? 각각 언제? | 타입 가드 필요 여부 |
| 8 | TS | Discriminated Union + Exhaustive check 방법 | `assertNever(x: never)` |
| 9 | TS | Branded Type이 필요한 이유 | 구조적 타이핑 한계 보완 |
| 10 | React | Virtual DOM이 "항상 빠르다"가 **틀린** 이유 | 선언적 모델 + 충분한 성능 |
| 11 | React | `useEffect` cleanup 실행 타이밍 3가지 | 언마운트, 의존성 변경 전, StrictMode |
| 12 | React | `useLayoutEffect` vs `useEffect` 차이 | 동기(paint 전) vs 비동기(paint 후) |
| 13 | React | 자동 배칭이 React 18에서 확장된 범위 | setTimeout, Promise 내에서도 배칭 |
| 14 | React | Error Boundary가 잡지 못하는 에러 4가지 | 이벤트 핸들러, 비동기, SSR, 자기 자신 |
| 15 | React | Context 리렌더링 문제와 해결법 | memo 관통, Context 분리, Zustand |
| 16 | React | key를 이용한 컴포넌트 상태 리셋 트릭 | key 변경 → 완전 새 마운트 |
| 17 | React | Fiber가 뭔지? Concurrent Mode와의 관계 | 작업 분할, lanes 우선순위 |
| 18 | RN | React vs RN 렌더링 파이프라인 차이? | DOM vs 네이티브뷰, Yoga vs CSS 엔진 |
| 19 | RN | New Arch 4요소? | Fabric, TurboModules, JSI, **Codegen** |
| 20 | RN | Hermes가 JSC보다 시작이 빠른 이유 | AOT 바이트코드 (.hbc), Hades GC |
| 21 | RN | Reanimated Worklet이 60fps 보장하는 원리 | Babel 플러그인, UI Thread JS 런타임 |
| 22 | RN | CodePush 현재 상태와 대안 | App Center 2025.3.31 종료, EAS Update |
| 23 | RN | FlatList 최적화 핵심 props 5가지 | windowSize, getItemLayout, keyExtractor 등 |
| 24 | RN | Deep Linking 종류? Universal Link vs URI Scheme | https:// vs custom:// |
| 25 | RN | AsyncStorage vs MMKV 차이 | 비동기/느림 vs 동기/30배 빠름 |
| 26 | RN+FL | Flutter와 RN의 렌더링 방식 근본 차이 | 자체 엔진(Skia) vs 네이티브 뷰 |
| 27 | RN+FL | SafeArea 처리: Flutter의 viewPadding vs RN의 insets | 키보드 관련 패딩 차이 |

---

*기존 `면접준비_매스프레소.md` 팩트 수정: 2026-04-01 완료*
*스터디 가이드 최종 업데이트: 2026-04-01 (기초 보완 + RN/Flutter 비교 추가)*
